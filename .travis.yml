language: objective-c
osx_image: xcode10.1
script: cd GameIcons; xcodebuild clean && xcodebuild build test -project GameIcons.xcodeproj -scheme GameIcons -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone XS Max,OS=12.1'
after_success:
- bash <(curl -s https://codecov.io/bash)
